import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'task_manager_app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;